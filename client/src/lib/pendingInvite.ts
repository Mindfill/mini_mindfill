/** localStorage key for a subscription-invite token grabbed from /invite/accept
 * before the user was logged in — resolved by use-auth.tsx on SIGNED_IN. */
export const PENDING_INVITE_TOKEN_KEY = "pending_invite_token";
